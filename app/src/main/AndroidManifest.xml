<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.twentyone.ttndevicesonmap">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TTNDevicesOnMap">
        <activity
            android:name=".TTNSettingsActivity"
            android:label="TTN Settings"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDZ94lU6AiQGTrAs3DngQjOiGgLQOb80nM" />

        <activity
            android:name=".TTNDeviceMapper"
            android:label="TTN Device On Map">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>